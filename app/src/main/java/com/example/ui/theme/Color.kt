package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Clean Minimalism Color Palette
val BrandPrimary = Color(0xFF0061A4)       // Clean Minimalist Precision Blue
val BrandPrimaryLight = Color(0xFF2B79C2)  // Crisp Slate Blue
val BrandPrimaryContainer = Color(0xFFD7E3FF) // Soft Airy Blue Container
val BrandOnPrimary = Color(0xFFFFFFFF)

// Secondary Colors - Precision Cyan / Teal
val BrandSecondary = Color(0xFF006877)     // Precision Cyan
val BrandSecondaryContainer = Color(0xFFCBE6FF)
val BrandOnSecondary = Color(0xFFFFFFFF)

// Tertiary Accent - Warm Amber / Gold
val BrandTertiary = Color(0xFFB45309)      // Warm Amber Accent
val BrandTertiaryContainer = Color(0xFFFEF3C7)
val BrandOnTertiary = Color(0xFFFFFFFF)

// Clean Minimalism Neutrals & Background (Airy Cool Off-White & Crisp Slate)
val LightBackground = Color(0xFFFAFBFF)    // Clean Minimalist Canvas Background
val LightSurface = Color(0xFFFFFFFF)       // Crisp White Card Surface
val LightSurfaceVariant = Color(0xFFF1F3F9)// Subtle Light Gray Card Variant
val LightBorder = Color(0xFFE2E8F0)        // Clean Hairline Border
val LightTextPrimary = Color(0xFF1A1C1E)   // Crisp High-Contrast Text
val LightTextSecondary = Color(0xFF44474E) // Subtle Secondary Text
val LightTextTertiary = Color(0xFF74777F)  // Minimal Muted Text

// Clean Minimalist Status Colors
val StatusReceivedText = Color(0xFF0061A4)
val StatusReceivedBg = Color(0xFFEFF6FF)

val StatusProcessingText = Color(0xFFEA580C)
val StatusProcessingBg = Color(0xFFFFF7ED)

val StatusReadyText = Color(0xFF15803D)
val StatusReadyBg = Color(0xFFF0FDF4)

val StatusCompletedText = Color(0xFF166534)
val StatusCompletedBg = Color(0xFFDCFCE7)

val StatusCancelledText = Color(0xFFDC2626)
val StatusCancelledBg = Color(0xFFFEF2F2)

val StatusPaidText = Color(0xFF15803D)
val StatusPaidBg = Color(0xFFF0FDF4)

val StatusFailedText = Color(0xFFDC2626)
val StatusFailedBg = Color(0xFFFEF2F2)

val StatusPendingText = Color(0xFFD97706)
val StatusPendingBg = Color(0xFFFFFBEB)

